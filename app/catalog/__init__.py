"""Catalog domain.

Subpackages:
- ``retrieval`` — compile, filter, rank, revalidate, execute
- ``index`` — durable Postgres catalog + brand cache + snapshots
- ``specs`` — watch specs and preference normalization
- ``media`` — storefront URLs, images, HTML fallback
- ``vision`` — photo identification and visual index
- ``category`` — Tray category resolver
"""
